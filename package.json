{
  "name": "jabir",
  "version": "1.0.0",
  "private": true,
  "description": "Jabir — Perfumer's Atelier (PWA + serverless API)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "stripe": "^16.0.0",
    "@supabase/supabase-js": "^2.45.0"
  }
}
